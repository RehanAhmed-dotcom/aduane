// updateRec?.map((res) => {
    //   if (res.is_recommended_req) {
    //     rec.map((cid) => {
    //       if (cid.is_recommended_req != res.is_recommended_req) {
    //         setbtn(true)
    //         const totalScores = rec?.reduce((previousScore, currentresult, index) => {
    //           console.log("---============", currentresult)
    //           previousScore + parseFloat(currentresult.vprice_count_increment_rc), 0
    //         });
    //         setfirst(totalScores)
    //         // alert("required select kiya ha")
    //       } else {
    //         // alert("select the required field")
    //       }
    //     })
    //   }

    // })